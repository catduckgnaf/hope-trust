SERVICE: ${self:service}
STAGE: ${self:provider.stage}
REGION: ${self:provider.region}
DB_HOST: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/db-host}
DB_NAME: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/db-name}
DB_USER: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/db-user}
DB_PASSWORD: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/db-password}
USER_POOL_ID: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/${self:custom.api}/user-pool-id}
APP_CLIENT_ID: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/${self:custom.api}/app-client-id}
CLIENT_USER_POOL_ID: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/client/user-pool-id}
BENEFITS_USER_POOL_ID: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/benefits/user-pool-id}
ACCESS_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/aws-access-key}
SECRET_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/aws-secret-key}
GIZMO_BASIC_USERNAME: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/gizmo-basic-username}
GIZMO_BASIC_PASSWORD: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/gizmo-basic-password}
GIZMO_API_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/gizmo-api-key}
GIZMO_SECRET: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/gizmo-secret}
BUDDY_BASIC_USERNAME: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/buddy-basic-username}
BUDDY_BASIC_PASSWORD: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/buddy-basic-password}
STRIPE_SECRET_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/stripe-secret-key}
STRIPE_WEBHOOK_SECRET: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/stripe-webhook-secret}
AX_REFRESH_TOKEN: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/ax-refresh-token}
HELLOSIGN_API_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/hello-sign-api-key}
HELLOSIGN_CLIENT_ID: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/hello-sign-client-id}
SENDGRID_API_KEY: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/sendgrid-api-key}
HUBSPOT_ACCESS_TOKEN: ${ssm:${self:custom.common.secrets_folder}/${self:provider.stage}/hubspot-access-token}
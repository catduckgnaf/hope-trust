import styled from "styled-components";
import { Row, Col } from "react-simple-flex-grid";

export const PaymentContainer = styled(Col)`
  margin-bottom: 20px;
  padding: 0 20px;
`;

export const PaymentMethodsMain = styled.div`
  max-height: 566px;
  overflow: auto;
`;

export const PaymentMethodsPadding = styled.div`

`;

export const PaymentMethodsInner = styled(Row)`

`;
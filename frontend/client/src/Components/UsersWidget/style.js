import styled from "styled-components";
import { Row, Col } from "react-simple-flex-grid";

export const UserListMain = styled(Row)`
  
`;
export const UsersListPadding = styled(Col)`
  
`;
export const UsersListInner = styled(Row)`
  
`;
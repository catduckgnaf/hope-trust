import styled from "styled-components";
import { theme } from "../../global-styles";

export const ReferralCodesMain = styled.div`
  
`;

export const ReferralCodesPadding = styled.div`
  padding: 20px;
`;

export const ReferralCodesInner = styled.div`
  background: ${theme.white};
  border-radius: 8px;
`;
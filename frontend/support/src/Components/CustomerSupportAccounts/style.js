import styled from "styled-components";
import { theme } from "../../global-styles";

export const UserAccountsMain = styled.div`
  
`;

export const UserAccountsPadding = styled.div`
  padding: 20px;
`;

export const UserAccountsInner = styled.div`
  background: ${theme.white};
  border-radius: 0 0 8px 8px;
`;
import styled from "styled-components";
import { theme } from "../../global-styles";

export const PartnerAccountsMain = styled.div`
  
`;

export const PartnerAccountsPadding = styled.div`
  padding: 20px;
`;

export const PartnerAccountsInner = styled.div`
  background: ${theme.white};
  border-radius: 0 0 8px 8px;
`;